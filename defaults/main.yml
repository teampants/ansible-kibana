---
# Ethernet interface to listen on. Defaults to loopback. Examples: lo, eth0, eth1
kibana_listen_interface: 'lo'
kibana_elasticsearch_url: 'http://localhost:9200'
kibana_issue_5524_workaround: true
